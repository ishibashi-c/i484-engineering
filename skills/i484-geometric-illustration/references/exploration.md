# Exploration / Selection

複数案の比較をユーザーが求めたときだけ読む。候補数はユーザー指定を優先し、各候補で変えるvariation axis（mass、negative space、rhythm、echo/reflectionなど）を明示する。全候補は同じ優先アンカーと制約を保持し、ランダムseed違いだけで候補を増やさない。

選択では`visual-review.md`の6つの観測軸を使い、最後にユーザーの好みと採用判断を優先する。候補ごとに変更したaxis、保持したアンカー、未解決のconcernを短く記録する。
